apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- helm.yaml

# Note: secret-example.yaml is not included in resources
# Apply it separately: kubectl apply -f secret-example.yaml

commonLabels:
  app.kubernetes.io/part-of: bitwarden-sync-secrets
  app.kubernetes.io/managed-by: kustomize 