apiVersion: batch/v1
kind: CronJob
metadata:
  name: rotation-vw
  namespace: rotation
spec:
  schedule: "0 3 * * 1"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: rotation
          restartPolicy: OnFailure
          containers:
            - name: rotate
              image: python:3-alpine
              command:
                - python3
                - /scripts/vw-rotate.py
              volumeMounts:
                - name: script
                  mountPath: /scripts
          volumes:
            - name: script
              configMap:
                name: rotation-vw
                defaultMode: 0555
