kind: Service
apiVersion: v1
metadata:
  name: flux-ui-pi-svc
  namespace: tailscale
spec:
  ports:
    - name: flux-ui-pi
      protocol: TCP
      port: 80
      targetPort: 9001
  # clusterIP: None
  type: ExternalName
  sessionAffinity: None
  externalName: ts-flux-ui-pi-wnhsv.tailscale.svc.cluster.local
---
kind: EndpointSlice
apiVersion: discovery.k8s.io/v1
metadata:
  name: flux-ui-pi
  namespace: tailscale
  labels:
    kubernetes.io/service-name: "flux-ui-pi-svc"
addressType: FQDN
ports:
  - name: flux-ui-pi
    port: 9001
    protocol: TCP
endpoints:
  - addresses:
      - "ts-flux-ui-pi-wnhsv.tailscale.svc.cluster.local"
    conditions:
      ready: true
---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: flux-ui-pi-ingress
  namespace: tailscale
  annotations:
    nginx.org/client-max-body-size: "1024m"
    kubernetes.io/ingress.allow-http: "true"
    acme.cert-manager.io/http01-edit-in-place: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1024m"
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  tls:
    - hosts:
        - flux-pi.lag0.com.br
      secretName: flux-pi-lag0
  rules:
    - host: flux-pi.lag0.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: 
                name: flux-ui-pi-svc
                port: 
                  number: 80
  ingressClassName: nginx