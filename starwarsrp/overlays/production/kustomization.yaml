apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gmod-starwarsrp-1
resources:
- configmap-image.yaml
- deployment.yaml
- service.yaml
- pvc.yaml
replacements:
- source:
    kind: ConfigMap
    name: starwarsrp-image-config
    fieldPath: data.image
  targets:
  - select:
      kind: Deployment
      name: starwarsrp-deployment
    fieldPaths:
    - spec.template.spec.containers.[name=starwarsrp-container].image
    options:
      create: true
