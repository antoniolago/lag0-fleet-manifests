apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movim-massacritica-pvc
  namespace: movim-massacritica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
