apiVersion: v1
kind: Service
metadata:
  name: movim
  namespace: movim-massacritica
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 443
  selector:
    app: movim