apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatwood-library
  namespace: chatwood
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1750Gi
  storageClassName: large-storage