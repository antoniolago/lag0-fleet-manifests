apiVersion: netbird.io/v1alpha1
kind: SetupKey
metadata:
  name: technitium-dns
  namespace: technitium-dns
spec:
  name: technitium-dns
  allowExtraDnsLabels: true
  ephemeral: false
  autoGroups: []
---
# SidecarProfile removido: o sidecar agora é gerenciado direto no deployment
# (technitium-dns-ton/helm.yaml postRenderers) com PVC em /var/lib/netbird
# para persistir a identidade do peer (IP estável entre restarts).

