apiVersion: v1
kind: Service
metadata:
  name: pihole
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/instance: pihole
    app.kubernetes.io/name: pihole
  ports:
  - protocol: TCP
    port: 53
    name: pihole-dns