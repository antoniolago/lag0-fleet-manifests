apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: harbor
  namespace: harbor
spec:
  interval: 24h
  url: https://helm.goharbor.io
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: harbor
  namespace: harbor
spec:
  releaseName: harbor-release
  targetNamespace: harbor
  install:
    createNamespace: true
  interval: 30m
  chart:
    spec:
      chart: harbor
      sourceRef:
        kind: HelmRepository
        name: harbor
        namespace: harbor
      interval: 12h
  values:
    adminPassword: AShjui1230u98SAJUIejh290183j #First time password
    global:
      defaultStorageClass: nfs-csi
    persistence:
      persistentVolumeClaim:
        registry: 
          storageClass: nfs-csi-pi-registry
        jobservice: 
          storageClass: nfs-csi
        trivy: 
          storageClass: nfs-csi
    expose:
      tls:
        enabled: true
        certSource: auto
        secret:
          secretName: "lag0-harbor-certificate"
      ingress:
        hosts:
          core: registry.lag0.com.br
        className: "nginx"
        annotations:
          # note different ingress controllers may require a different ssl-redirect annotation
          # for Envoy, use ingress.kubernetes.io/force-ssl-redirect: "true" and remove the nginx lines below
          ingress.kubernetes.io/ssl-redirect: "true"
          ingress.kubernetes.io/proxy-body-size: "0"
          nginx.ingress.kubernetes.io/ssl-redirect: "true"
          nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx:
      image:
        repository: ghcr.io/octohelm/harbor/nginx-photon:v2.12.0
    portal:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-portal:v2.12.0
    registry:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-registry:v2.12.0
    trivy:
      image:
        repository: ghcr.io/octohelm/harbor/trivy-adapter:v2.12.0
    jobservice:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-jobservice:v2.12.0
    chartmuseum:
      image:
        repository: ghcr.io/octohelm/harbor/chartmuseum-photon:v2.12.0
    clair:
      image:
        repository: ghcr.io/octohelm/harbor/clair-photon:v2.12.0
    database:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-db:v2.12.0
    redis:
      image:
        repository: ghcr.io/octohelm/harbor/redis-photon:v2.12.0
    core:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-core:v2.12.0
    exporter:
      image:
        repository: ghcr.io/octohelm/harbor/harbor-exporter:v2.12.0