---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-all-tailscale
  namespace: tailscale
spec:
  endpointSelector: {}
  egress:
  - toEntities:
    - all
  ingress:
  - fromEntities:
    - all
