apiVersion: v1
kind: Service
metadata:
  name: hermes-api
  namespace: hermes
  labels:
    app: hermes
spec:
  type: ClusterIP
  selector:
    app: hermes
  ports:
  - port: 8642
    targetPort: api
    name: api
    protocol: TCP
