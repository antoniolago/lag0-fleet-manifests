apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: hermes
  labels:
    app: open-webui
spec:
  type: ClusterIP
  selector:
    app: open-webui
  ports:
  - port: 80
    targetPort: web
    name: web
    protocol: TCP
