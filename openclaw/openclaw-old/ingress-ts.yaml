apiVersion: v1
kind: Service
metadata:
  name: openclaw-tailscale
  namespace: openclaw
  annotations:
    tailscale.com/expose: "true"
spec:
  selector:
    app: openclaw
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
  loadBalancerClass: tailscale
