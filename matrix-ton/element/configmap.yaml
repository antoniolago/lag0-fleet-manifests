apiVersion: v1
kind: ConfigMap
metadata:
  name: element-config
  namespace: matrix
data:
  config.json: |
    {
      "default_server_config": {
        "m.homeserver": {
          "base_url": "https://matrix.lag0.com.br"
        }
      },
      "branding": {
        "authFooterLinks": []
      },
      "defaultCountryCode": "GB",
      "showLabsSettings": true,
      "features": {
        "feature_spotlight": true
      }
    }
