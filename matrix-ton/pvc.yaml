apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: conduit-data
  namespace: matrix
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: large-storage
