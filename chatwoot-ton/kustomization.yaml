apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: chatwoot
resources:
  - namespace.yaml
  - helm.yaml
  - virtualservice.yaml
  - oidc  # OIDC SSO Extension via Pocket ID

patches:
  - path: oidc-patch.yaml
    target:
      kind: HelmRelease
      name: chatwoot
