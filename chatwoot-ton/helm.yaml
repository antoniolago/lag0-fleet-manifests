apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: chatwoot
  namespace: chatwoot
spec:
  interval: 24h
  url: https://chatwoot.github.io/charts
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chatwoot
  namespace: chatwoot
spec:
  suspend: false
  releaseName: chatwoot
  targetNamespace: chatwoot
  interval: 30m
  chart:
    spec:
      chart: chatwoot
      # version: 0.9.0
      sourceRef:
        kind: HelmRepository
        name: chatwoot
        namespace: chatwoot
      interval: 12h
  values:
    env:
      FRONTEND_URL: "https://chatwoot.lag0.com.br"
      FORCE_SSL: "true"
      INSTALLATION_ENV: helm
    postgresql:
      enabled: true
      auth:
        database: chatwoot_production
    # Using built-in Redis (sub-chart)
    redis:
      enabled: true
  valuesFrom:
    - kind: Secret
      name: chatwoot-secrets
