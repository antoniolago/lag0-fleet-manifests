apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatwoot-library
  namespace: chatwoot
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1750Gi
  storageClassName: large-storage