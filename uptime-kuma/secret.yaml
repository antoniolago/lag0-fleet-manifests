---
apiVersion: v1
kind: Secret
metadata:
  name: uptime-kuma
  namespace: uptime-kuma
type: Opaque
stringData:
  # Optional: set a reverse proxy username/password for the /status page
  # Leave empty if using Authelia/OIDC at the Ingress level.
  UPTIME_KUMA_USERNAME: ""
  UPTIME_KUMA_PASSWORD: ""
