---
apiVersion: v1
kind: Secret
metadata:
  name: signoz
  namespace: signoz
type: Opaque
stringData:
  # ClickHouse admin password
  CLICKHOUSE_PASSWORD: "changeme-signoz-clickhouse-password"
  # SigNoz ingestion key (optional — service uses internal comms by default)
  SIGNOZ_INGESTION_KEY: ""
