apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rook-ceph
resources:
# - cluster.yaml
# - cephfs.yaml
# - cephnfs.yaml
# - storageclass.yaml
- helm.yaml
# - dashboard-service-patch.yaml

# patchesStrategicMerge:
# - dashboard-service-patch.yaml