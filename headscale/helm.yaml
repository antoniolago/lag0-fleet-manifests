apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRelease
metadata:
  name: headscale
  namespace: headscale
spec:
  interval: 10m
  url: https://charts.nbcloud.io
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headplane
  namespace: headscale
spec:
  releaseName: headscale
  targetNamespace: headscale
  install:
    createNamespace: true
  interval: 30m
  chartRef:
    chart: headplane
    kind: HelmRepository
    name: headplane